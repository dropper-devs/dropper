/** Named timing constants for the hero demo (previously inline magic numbers). */

/** Simulated upload run: progress ring 0 → 100%. */
export const UPLOAD_DURATION_MS = 1500;

/** Grace period before a hovered-away confirm pair reverts (app: ~1s). */
export const REVERT_MS = 1000;

/** How long the "Copied ✓" / "Copy failed" feedback stays up. */
export const COPY_FEEDBACK_MS = 1200;

/** Cadence of the periodic "you can drag me" chip nudge. */
export const NUDGE_INTERVAL_MS = 10000;

/** Duration of a single wiggle nudge. */
export const NUDGE_DURATION_MS = 700;

/** Refresh-button spin animation length. */
export const REFRESH_SPIN_MS = 650;
