# Dropper distribution configuration.

VERSION="0.1.3"

APP_NAME="Dropper"
APP_DISPLAY_NAME="Dropper"
EXECUTABLE_NAME="Dropper"
EXECUTABLE_PRODUCT="Dropper"
RESOURCE_BUNDLE_NAME="Dropper_Dropper.bundle"
BUNDLE_ID="com.temeculadsp.dropper"
MIN_MACOS="14.0"

MAC_SIGNING_IDENTITY="Developer ID Application: John Wheeler (2L2A6AD7C2)"
MAC_NOTARIZE_PROFILE="temeculadsp-notarize"

# Cloudflare R2 destination. Wrangler supplies authenticated remote access.
R2_BUCKET="dropper-page"
R2_PATH="installers"
